import { StyleSheet } from 'react-native';

//import theme from '../../../theme';

//const { color, margins } = theme;

export default StyleSheet.create({
  buttonContainer: {
    marginTop: 40,
    width: '100%',
    flexDirection: 'row',
    justifyContent: 'flex-end',
    marginBottom: 40,
  },
  button: {
    color: '#ffffff',
    fontSize: 22,
    fontWeight: '500',
    marginTop: 20,
  },
});
